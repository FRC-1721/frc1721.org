{
  "name": "frc1721.org",
  "source": "src/index.html",
  "browserslist": "> 0.1%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel src/*.html",
    "build": "parcel build src/*.html",
    "build:inject": "node -e \"const fs = require('fs'); const commit = require('child_process').execSync('git rev-parse --short HEAD').toString().trim(); let html = fs.readFileSync('./dist/index.html', 'utf8'); html = html.replace(/__COMMIT__/g, commit); fs.writeFileSync('./dist/index.html', html);\""
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "posthtml-include": "^2.0.1"
  }
}
